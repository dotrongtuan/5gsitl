name: mmtc_burst
objective: Emulate bursty small-packet slice workload
scenario_class: slicing
nr_config_profile: configs/nr_profiles/mmtc_dense.yaml
channel_config: configs/channel/bypass.yaml
traffic_config: { mode: udp-latency, duration_s: 20, bitrate: 1M, target: 10.45.0.1 }
capture_config: { enabled: true }
slice_profile: configs/slicing/mmtc.yaml
expected_kpis: { max_loss_pct: 3, max_avg_rtt_ms: 80 }
report_metadata: { group: slicing, tags: [mmtc, burst] }
